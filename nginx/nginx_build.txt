#!/bin/bash
export NGINX_HOST="YOUR-LOCAL-IP"
export NGINX_PORT="80"
export APP_HOST="YOUR-LOCAL-IP"
export APP_PORT="8000"
#docker build . -f nginx/Dockerfile --build-arg NGINX_HOST --build-arg NGINX_PORT --build-arg APP_HOST --build-arg APP_PORT -t testweb_nginx
docker build . --build-arg NGINX_HOST --build-arg NGINX_PORT --build-arg APP_HOST --build-arg APP_PORT -t testweb_nginx
